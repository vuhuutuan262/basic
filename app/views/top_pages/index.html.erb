<div class="col-md-10 col-md-offset-1">
  <div class="col-md-12">
    <%= render "shared/slide" %>
  </div>
  <div class="col-md-12">
    <div class="col-md-10 col-md-offset-1">
      <div class="text-left" style="border-bottom: solid 2px black; margin-bottom: 10px">
        <h2>San pham</h2>
      </div>
      <%= render "shared/product" %>
      <%= render "shared/product" %>
      <%= render "shared/product" %>
      <%= render "shared/product" %>

      <div class="text-left" style="border-bottom: solid 2px black; margin-bottom: 10px">
        <h2>San pham</h2>
      </div>
      <%= render "shared/product" %>
      <%= render "shared/product" %>
      <%= render "shared/product" %>
      <%= render "shared/product" %>
      <div class="text-left" style="border-bottom: solid 2px black; margin-bottom: 10px">
        <h2>San pham</h2>
      </div>
      <%= render "shared/product" %>
      <%= render "shared/product" %>
      <%= render "shared/product" %>
      <%= render "shared/product" %>
    </div>
  </div>
</div>
</div>
